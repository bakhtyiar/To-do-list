// import React from "react";

export default function header() {
	return (
		<header className="App-header" style={styles}>
			<h1>TO DO APPLICATION</h1>
			<p>create-react-app, MUI, npm</p>
		</header>
	);
}

const styles = {
	textAlign: "center",
};
